spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  http:
    encoding:
      force: true
      charset: utf-8
      enabled: true
      
server:
  tomcat:
    uri-encoding: UTF-8